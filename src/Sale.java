public class Sale {

    // some functions to calculate the sale price: 15%, 20%,
    public static double calculateSalePrice(double price, int discount) {
        return price * (100 - discount) / 100;
    }

    // return an array of items that're on sale?

    // some statistics? like revenue/day? cost/day?
}
